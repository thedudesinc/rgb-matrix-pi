# Sample udev rule to give the `input` group access to a specific keyboard.
# Replace ATTRS{idVendor}/ATTRS{idProduct} with values from `lsusb` or use ATTRS{name}.

# Example by VID/PID (uncomment and set correct values):
# SUBSYSTEM=="usb", ATTR{idVendor}=="046d", ATTR{idProduct}=="c52b", MODE="0660", GROUP="input"

# Example using the input device name (Logitech TK820):
# Match keyboards by name and give group `input` read/write
SUBSYSTEM=="input", ATTRS{name}=="Logitech TK820", MODE="0660", GROUP="input"

# If your device exposes a hidraw node and you prefer matching hidraw:
# KERNEL=="hidraw*", ATTRS{manufacturer}=="Logitech", ATTRS{product}=="TK820", MODE="0660", GROUP="input"

# After installing this file to /etc/udev/rules.d/99-rgb-matrix-input.rules,
# run:
#   sudo udevadm control --reload
#   sudo udevadm trigger
# and add your user to the `input` group: `sudo usermod -aG input youruser` then re-login.
